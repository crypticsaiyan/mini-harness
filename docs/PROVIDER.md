# `/provider`

This folder contains the files which help to communicate with the provider (OpenRouter).

## Files:

1. `client.ts`: basic OpenRouter config
2. `types.ts`: type definitions
3. `normalize.ts`: normalization (OpenRouter types -> Custom types)
4. `complete.ts`: main communication with the model
5. `index.ts`: bundle
